# 나만의 비서 📅

일정 관리 Flask 웹 애플리케이션입니다. **PC(데스크톱)**와 **스마트폰(모바일)** 화면을 분리하여 제공합니다.

## 기능

✅ **일정 생성/조회/수정/삭제** (CRUD)
✅ **완료 상태 표시** (체크박스)
✅ **데스크톱 최적화** - 넓은 화면에서 정보 밀도
✅ **모바일 최적화** - 터치 친화적, 반응형 디자인
✅ **일정 검색** - 제목, 설명으로 빠른 검색
✅ **시간대 설정** - 모바일에서 '내일', '다음주' 등 단축 설정

## 프로젝트 구조

```
MySecretary/
├── app.py                      # Flask 메인 앱 (라우트 및 API 정의)
├── models.py                   # SQLAlchemy 데이터 모델
├── db_config.py                # 데이터베이스 설정 (SQLite)
├── requirements.txt            # Python 의존성
├── templates/
│   ├── desktop/                # 데스크톱 버전 HTML
│   │   ├── index.html         # 일정 목록 (캘린더 + 리스트 뷰)
│   │   ├── create.html        # 일정 생성 폼
│   │   └── edit.html          # 일정 수정 폼
│   └── mobile/                 # 모바일 버전 HTML
│       ├── index.html         # 일정 목록 (리스트 뷰, 터치 최적)
│       ├── create.html        # 일정 생성 폼 (모바일 최적)
│       └── edit.html          # 일정 수정 폼 (모바일 최적)
├── static/
│   ├── css/                    # 스타일시트 (향후 추가)
│   └── js/                     # JavaScript (향후 추가)
└── README.md                   # 이 파일

```

## 시작하기

### 1. 가상 환경 설정하기

```bash
cd MySecretary
python -m venv venv
```

**Windows:**
```bash
venv\Scripts\activate
```

**Mac/Linux:**
```bash
source venv/bin/activate
```

### 2. 의존성 설치

```bash
pip install -r requirements.txt
```

### 3. 앱 실행

```bash
python app.py
```

출력:
```
✅ 데이터베이스 초기화 완료
 * Running on http://127.0.0.1:5000
```

### 4. 브라우저에서 접속

- **데스크톱 버전**: http://localhost:5000/desktop
- **모바일 버전**: http://localhost:5000/mobile
- 또는 http://localhost:5000 (기본값: 데스크톱)

---

## 사용 방법

### 데스크톱 (PC)
1. `/desktop` 접속
2. **+ 새 일정** 버튼으로 일정 추가
3. 제목, 설명, 시작/종료 시간 입력
4. 저장하면 일정 목록에 표시
5. 수정/삭제 가능

### 모바일 (스마트폰)
1. `/mobile` 접속
2. **+ 버튼** (하단 FAB) 클릭
3. 일정 입력 (모바일 최적화된 폼)
4. '내일', '다음주', '다음달' 단축 선택 가능
5. 하단 네비게이션으로 새로고침/데스크톱 버전 전환

---

## API 엔드포인트

모든 API는 JSON을 사용합니다.

### 일정 조회

```
GET /api/schedule          # 모든 일정 조회
GET /api/schedule/<id>     # 특정 일정 조회
```

### 일정 생성

```
POST /api/schedule
Content-Type: application/json

{
  "title": "회의",
  "description": "팀 미팅",
  "start_date": "2026-02-15T10:00",
  "end_date": "2026-02-15T11:00"
}
```

### 일정 수정

```
PUT /api/schedule/<id>
Content-Type: application/json

{
  "title": "수정된 제목",
  "is_completed": true
}
```

### 일정 삭제

```
DELETE /api/schedule/<id>
```

---

## 데이터베이스

SQLite를 사용합니다. 첫 실행 시 `schedule.db` 파일이 생성됩니다.

**Schedule 테이블:**
- `id` (Integer, PK)
- `title` (String, 필수)
- `description` (Text)
- `start_date` (DateTime, 필수)
- `end_date` (DateTime)
- `is_completed` (Boolean, 기본값: False)
- `created_at` (DateTime)
- `updated_at` (DateTime)

---

## 기술 스택

| 항목 | 기술 |
|------|------|
| Backend | Flask, SQLAlchemy |
| Database | SQLite |
| Frontend | HTML5, CSS3, Vanilla JavaScript |
| 스타일 | 반응형 CSS (Mobile-first) |

---

## 다음 단계 (향후 개선사항)

- 📱 반복 일정 (매주, 매달)
- 🔔 알림 기능
- 👥 일정 공유
- 📊 일정 통계
- 🌙 다크 모드
- 🌍 다국어 지원

---

## 문제 해결

### 포트 이미 사용 중
```bash
python app.py --port 5001
```

### 데이터베이스 초기화
`schedule.db` 파일을 삭제하고 `app.py` 다시 실행

### 모바일에서 접속 안 됨
- `app.run()` 부분을 `app.run(host='0.0.0.0')`로 수정
- 같은 네트워크에서 `http://<PC_IP>:5000`로 접속

---

## 라이선스

MIT License

---

**작성일**: 2026년 2월
**버전**: 1.0.0
